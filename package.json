{
  "name": "lattice",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"test/**/*.test.mjs\"",
    "check": "npm run typecheck && npm test && npm run words && npm run doctor && npm run build && npm run size && npm run e2e",
    "doctor": "powershell -NoProfile -ExecutionPolicy Bypass -File C:/dev/gamedev-notes/scripts/doctor.ps1 -Repo lattice -Quiet",
    "words": "node scripts/check-us-english.mjs",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.e2e.json",
    "e2e": "node scripts/check-e2e-browser.mjs && playwright test",
    "size": "node scripts/check-bundle-size.mjs",
    "size:update": "node scripts/check-bundle-size.mjs --update",
    "film": "node scripts/filmstrip.mjs",
    "econ": "node scripts/econ.mjs",
    "store": "npm run build && node scripts/store.mjs"
  },
  "dependencies": {
    "three": "0.166.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.4.1",
    "@types/three": "^0.166.0",
    "typescript": "^7.0.2",
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^1.3.0"
  }
}
